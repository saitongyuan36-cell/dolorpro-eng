name: Database Migrations\n\non:  \n  push: \n    paths: \n      - db/migrations/** \n  pull_request: \n    paths: \n      - db/migrations/** \n\njobs: \n  test: \n    runs-on: ubuntu-latest \n    steps: \n      - name: Checkout code \n        uses: actions/checkout@v2 \n      - name: Set up PostgreSQL \n        uses: harmon758/postgresql-action@v1 \n        with: \n          postgresql version: '12' \n          database: 'test_db' \n          user: 'postgres' \n          password: 'password' \n      - name: Run migrations \n        run: | \n          psql -h localhost -U postgres -d test_db -f db/migrations/* \n      - name: Validate migrations \n        run: | \n          # Add commands to validate database schema here\n          echo 'Validating migrations...' \n\n
